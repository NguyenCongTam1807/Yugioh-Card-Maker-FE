import 'package:flutter/material.dart';

class TrapCard extends StatefulWidget {
  const TrapCard({Key? key}) : super(key: key);

  @override
  State<TrapCard> createState() => _TrapCardState();
}

class _TrapCardState extends State<TrapCard> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
